// Task 6 - Filler Code
function greet(name) {
  return `Hello, ${name}!`;
}

console.log(greet("World"));

const numbers = [1, 2, 3, 4, 5];
const doubled = numbers.map((n) => n * 2);
console.log(doubled);
